<%= form_with model: @user, url: user_path(@user), method: :patch do |f| %>
	<%= f.label :image %>
	<%= f.file_field :image, placeholder: "プロフィール画像" %>
	
	<%= f.label :handle %>
	<%= f.text_field :handle %>

	<%= f.label :name %>
	<%= f.text_field :name %>
	
	<%= f.submit "変更" %>
<% end %>
